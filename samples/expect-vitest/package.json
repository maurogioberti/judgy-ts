{
  "name": "@judgy-ts/sample-expect-vitest",
  "version": "0.1.0",
  "type": "module",
  "description": "Vitest sample for Judgy TypeScript expect matchers.",
  "dependencies": {
    "@judgy-ts/core": "workspace:*",
    "@judgy-ts/expect": "workspace:*",
    "@judgy-ts/ollama": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  },
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
